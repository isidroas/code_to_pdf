%- extends "base.tex"

%- block body
\maketitle

\dirtree{%
%- for node in nodes
  %- if node.is_file
  .\VAR{node.depth} {\VAR{node.name|e}}\DTcomment{\pageref{sec:\VAR{node.path}}}.
  %- else
  .\VAR{node.depth} {\bf \color{blue} \VAR{node.name|e}}\DTcomment{}.
  %- endif
%- endfor
}


%- for code in codes
  %- if break_on_each_section
\newpage
  %- endif
\section*{\VAR{code.rel|e}}\label{sec:\VAR{code.abs}}
  %- if filename_in_header
\markboth{\VAR{code.rel|e}}{\VAR{code.rel|e}} %# source https://tex.stackexchange.com/a/222385
\thispagestyle{HideSectionInHeader}
  %- endif
\inputminted[]{\VAR{code.lang}}{\VAR{code.abs}}
%- endfor

%- endblock
